// check "readln, eof, stoa, atos, and random." creation but without microLIR
class C1
{
	static void main(string[] args)
	{
		string s =readln();
		eof();
		int[] i = stoa(s);
		string s2 = atos(i);
		int r = random(10);
	}
}
